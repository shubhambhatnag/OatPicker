/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
reactStrictMode = false

module.exports = {
    distDir: 'out', // Make sure this matches your actual output directory
    // ... other configurations ...
  };